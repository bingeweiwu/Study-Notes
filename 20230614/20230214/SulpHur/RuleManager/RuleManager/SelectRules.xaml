<Window x:Class="RuleManager.SelectRules"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SelectRules" Height="512" Width="331" WindowStyle="ToolWindow" WindowStartupLocation="CenterScreen">
    <Grid>
        <ScrollViewer Margin="0,0,0,43">
            <ListView x:Name="RuleList" ItemsSource="{Binding Path=RuleList1}" Margin="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Margin="0,3,0,0">
                            <Grid>
                                <CheckBox Margin="5,3,0,0" IsChecked="{Binding Path=IsSelected}"></CheckBox>
                                <TextBlock FontSize="14" Margin="20,0,0,0" Text="{Binding Path=Name}" FontWeight="Bold" FontFamily="Segoe UI" VerticalAlignment="Top"></TextBlock>
                            </Grid>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        <Button Content="Rescan" Margin="0,0,17,12" Name="btnRescanBySelectedRules" Click="btnRescanBySelectedRules_Click" HorizontalAlignment="Right" Width="75" Height="23" VerticalAlignment="Bottom" />
    </Grid>
</Window>
